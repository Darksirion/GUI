package Codehaufen;

/**
 * Created by Darksirion on 15.09.15.
 */
public class Controller {

    /*
        @FXML
    public void handleButtonSettingAction(ActionEvent e) throws IOException {

        Parent root;
        Stage dialogStage;



        if(e.getSource()==btnSetting){
            FXMLLoader loader = new FXMLLoader();

            dialogStage= new Stage();
            root = loader.load((getClass().getResource("setting.fxml")));
            dialogStage.setScene(new Scene(root));
            dialogStage.initModality(Modality.WINDOW_MODAL);
            dialogStage.initOwner(btnSetting.getScene().getWindow());

            SettingController controller = loader.<SettingController>getController();
            //controller.setDialogStage(dialogStage);

            //  controller.setDialogStage(dialogStage);

            dialogStage.showAndWait();

        }


        // else{
        //     stage=(dialogStage) btnSettingClose.getScene().getWindow();
        //     stage.close();
        // }

    }

     */


    //  @FXML
    //  public void handleMenuButtonLang(ActionEvent e) throws IOException {
//
    //      menuBarObject1.setOnAction(new EventHandler<ActionEvent>() {
    //       public void handle(ActionEvent e) {
    //            System.out.println("About");
    //           }
    //      });
    //  if(e.getSource()==menuBarObject1){
//            out();
    //      }
//
    //      else if(e.getSource()==menuBarObject2){
    //        System.out.println("this is chap 1");
//
    //          //get reference to the button's stage
//            stage = (Stage) root.getScene().getWindow();
    //load up OTHER FXML document
    //          root = FXMLLoader.load(getClass().getResource("setting.fxml"));
//
//            //create a new scene with root and set the stage
//            Scene scene = new Scene(root);
//            stage.setScene(scene);
//            stage.show();
//        }
//
    //      else if(e.getSource()==menuBarObject3){
    //        System.out.println("this is chap 2");
    //  }
//
    //  }

}

class Controller2 {
    /*
    public void handleButtonSettingAction(ActionEvent e) throws IOException {

        FXMLLoader fxmlLoader = new FXMLLoader();
        fxmlLoader.setLocation(getClass().getResource("setting.fxml"));
        fxmlLoader.load();
        Parent root = fxmlLoader.getRoot();

        Stage setting_modal_dialog=new Stage(StageStyle.DECORATED);
        setting_modal_dialog.initModality(Modality.WINDOW_MODAL);
        setting_modal_dialog.initOwner(primaryStage);
        Scene scene= new Scene(root);

        SettingController settingController = (SettingController)fxmlLoader.getController();
        settingController.setStage(setting_modal_dialog);
        setting_modal_dialog.setScene(scene);
        setting_modal_dialog.show();


    }
    */

    /* Parent root;


        if (e.getSource() == btnSetting) {
            FXMLLoader loader = new FXMLLoader();

            Stage dialogStage = new Stage();
            root = FXMLLoader.load((getClass().getResource("setting.fxml")));
            dialogStage.setScene(new Scene(root));
            dialogStage.initModality(Modality.WINDOW_MODAL);
            dialogStage.initOwner(btnSetting.getScene().getWindow());

            SettingController controller = loader.<SettingController>getController();

            dialogStage.showAndWait();

        }

         else{
            //TODO
         }
*/
}

class Controller3 {
        /*

         public void loadTreeItems(String... rootItems) {
        TreeItem<String> root = new TreeItem<>("Snippet");
        root.setExpanded(true);

        for (String itemString : rootItems) {
            root.getChildren().add(new TreeItem<String>(itemString));
        }

        treeView.setRoot(root);

         */
}


class Controller4 {
    /*






     */
}